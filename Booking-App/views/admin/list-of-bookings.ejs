<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>List of Bookings</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
  <link rel="stylesheet" href="/css/list-styles.css">
</head>

<body>
  <header id="main-header" class="bg-primary text-white p-1 mb-3">
    <div class="container">
      <h1 id="header-title">ADMIN LIST OF BOOKINGS</h1>
    </div>
  </header>
  <div class="container">
    <div class="card">
      <div class="card-body">
        <div class="title-box">
          <h4 class="title">Access Your Bookings Here</h4>
        </div>

        <ul class="booking-list">
          <% if (bookings.length > 0) { %>
          <!-- List items will be added here dynamically -->
          <% for (let booking of bookings) { %>
          <li class="booking-item">
            <div class="card-container">
              <div class="card">
                <div class="card-body">
                  <h5 class="card-title"><%= booking.name %></h5>
                  <p class="card-text">Email Id: <%= booking.email %></p>
                  <p class="card-text">Phone Number: <%= booking.phoneNumber %></p>
                  <p class="card-text">Time: <%= booking.slot %></p>
                  <form action="/admin/cancel-booking" method="POST">
                    <input type="hidden" value="<%= booking.id %>" name="bookingId">
                    <input type="submit" class="btn btn-danger" value="Cancel Booking">
                  </form>
                </div>
              </div>
            </div>
          </li>
          <% } %>
          <!-- Repeat this structure for each booking -->
          <% } else { %>
          <h1>No Bookings Found!</h1>
          <% } %>
        </ul>

      </div>
    </div>
  </div>
</body>

</html>